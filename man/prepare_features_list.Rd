% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_features_list.R
\name{prepare_features_list}
\alias{prepare_features_list}
\title{Prepare a features list for enrichment or pathway analysis}
\usage{
prepare_features_list(
  features_table,
  fdr_cutoff = 1,
  cor_cutoff = 0,
  fields = c("Feature_ID"),
  save_file = FALSE,
  prefix = NULL,
  outdir = "results/features_lists",
  colnames = FALSE
)
}
\arguments{
\item{features_table}{A data frame containing feature-level results for one dimension.
Must include at least \code{correlation} and \code{p.adj}.}

\item{fdr_cutoff}{Numeric. Maximum adjusted p-value allowed (default = 1, i.e., no filter).}

\item{cor_cutoff}{Numeric. Minimum absolute correlation required (default = 0).}

\item{fields}{Character vector of column names to return (default = "Feature_ID").}

\item{save_file}{Logical. Whether to save the filtered list as a TSV file (default = FALSE).}

\item{prefix}{Character. Prefix for the output file name (default = inferred from \code{Dimension} column if available).}

\item{outdir}{Character. Output directory for the saved file (default = "results/features_lists").}

\item{colnames}{Logical. Whether to include column names in the saved TSV file (default = FALSE).}
}
\value{
A tibble containing the filtered features with the requested fields.
}
\description{
Filters and extracts a list of features for enrichment analyses
(ORA, MSEA, ChemRich, etc.) based on correlation and adjusted p-value thresholds.
}
\examples{
# Example feature table for one dimension
features_example <- data.frame(
  Feature_ID = paste0("HMDB0000", 161:170),
  correlation = c(0.35, 0.42, 0.12, 0.29, -0.25, -0.32, -0.18, 0.10, 0.05, -0.02),
  p.adj = c(0.01, 0.04, 0.30, 0.10, 0.25, 0.60, 0.80, 0.05, 0.90, 0.40),
  chemical_families = rep(c("Amino Acids", "Biogenic Amines"), each = 5),
  stringsAsFactors = FALSE
)

# 1. Filtered list for ORA-style enrichment
ora_list <- prepare_features_list(
  features_table = features_example,
  fdr_cutoff = 0.25,
  cor_cutoff = 0.1,
  fields = c("Feature_ID")
)
ora_list

# 2. Full list for MSEA-style enrichment
msea_list <- prepare_features_list(
  features_table = features_example,
  fdr_cutoff = 1,
  cor_cutoff = 0,
  fields = c("Feature_ID", "correlation", "chemical_families")
)
msea_list
}
